from collections import Counter

for i in range(int(input())):
  n,k = map(int, input().split())
  a = input()
  c = list(Counter(a).values())
  xc = list(Counter(a).keys())
  odd = 0
  for j in range(len(c)):
    if c[j] %2!=0:
      odd +=1
  if odd <= k+1:
    print("YES")
  else:
    print("NO")